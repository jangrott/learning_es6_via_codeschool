{
  "name": "learning_es6",
  "description": "Learning es6 via codeschool",
  "version": "1.0.0",
  "scripts": {
    "build": "babel --presets es2015,stage-0 --plugins transform-runtime src/ -d build/"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-plugin-transform-runtime": "^6.6.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
